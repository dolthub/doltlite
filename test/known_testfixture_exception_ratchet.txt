# Ceiling on testfixture exceptions. check_testfixture_exception_inventory.sh
# fails if any count moves in either direction: growing means more of upstream's
# suite is excluded from CI, and shrinking must be recorded here or the gain
# silently drifts back later.
#
# Counts are per gate -- one divergent assertion, or one expected crash.
# Lowering a number is the point. Raising one is a deliberate act that belongs
# in a PR description.
gates 4702
intentional 3438
unsupported 1252
harness 11
engine-gap 1
