# Suites that may terminate before the testfixture summary. This inventory does
# not authorize a passing result; run_testfixture.sh requires the exact contract
# in known_testfixture_terminations.txt. Unbucketed suites require replacement
# coverage in known_testfixture_crash_coverage.txt.

alter2       # raw file-format setup prevents the db command from opening
backup_ioerr # cap-truncated page-copy fault sequence; unbucketed
btreefault   # rowid setup failure cascades into the fault simulator
corrupt4     # raw-offset outcome varies by build; unbucketed
crash8       # crash-simulation timing is nondeterministic; unbucketed
dbstatus     # echo-vtab cleanup after schema reset
incrblob     # rowid assumption on a primary-key table
ioerr2       # exceeds the per-file timeout; unbucketed
malloc       # cap-truncated OOM fault sequence; unbucketed
malloc3      # unstable OOM fault-point set; unbucketed
multiplex2   # incompatible multiplex VFS setup
pagerfault   # unstable OOM fault-point set; unbucketed
pagerfault2  # unstable OOM fault-point set; unbucketed
vtab1        # echo-vtab cleanup after schema reset
