# Flaky testfixture tests: pass on some runs and fail on others, only on the
# GitHub CI runner (never reproducible locally — see investigation in #1132).
# run_testfixture.sh ignores these in both directions: a listed test (and any
# subtest under the prefix) is never an unexpected failure nor a now-fixed
# divergence, so an intermittent flip can't red the build either way.
#
# Format: <test_file> <test-name-prefix>  # reason
#
# select9-2.1.6 is "... UNION ALL ... ORDER BY 2 COLLATE reverse, 1": the
# NULL-column-2 group's tie-order is correct in isolation but, after the
# test's earlier index activity, comes out in a runner-dependent order — it
# passes sometimes and fails other times, only on the github runner.
select9 select9-2.1.6
